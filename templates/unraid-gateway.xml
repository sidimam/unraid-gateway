<?xml version="1.0"?>
<Container version="2">
  <Name>unraid-gateway</Name>
  <Repository>ghcr.io/sidimam/unraid-gateway:latest</Repository>
  <Registry>https://github.com/sidimam/unraid-gateway/pkgs/container/unraid-gateway</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/sidimam/unraid-gateway/issues</Support>
  <Project>https://github.com/sidimam/unraid-gateway</Project>
  <Overview>Single authenticated HTTPS port for mobile clients: a streaming file API over your shares (list, download with Range, resumable upload, move/copy/delete, change feed for iOS File Provider) plus a proxy to the Unraid GraphQL API and a small web UI to test it from a browser. Authentication uses your Unraid API key (Settings &gt; Management Access &gt; API Keys), optionally plus an Unraid user and password: the user then gets exactly the share permissions configured in Unraid (public / secure / private, read and write lists), verified against the Unraid SMB service. Run behind a reverse proxy with a valid certificate (Nginx Proxy Manager, SWAG) or Cloudflare Tunnel. Mount only the shares you want reachable from your phone: documents, media and downloads are preconfigured, add any other share as an extra Path under /data/&lt;name&gt;. Do not map Time Machine shares.</Overview>
  <Category>Cloud: Network:FTP Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:8484]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/sidimam/unraid-gateway/main/templates/unraid-gateway.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/sidimam/unraid-gateway/main/assets/icon.png</Icon>
  <ExtraParams>--health-cmd "curl -fsS http://127.0.0.1:8484/healthz || exit 1" --health-interval 30s</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Beta>False</Beta>
  <Date>2026-09-11</Date>
  <Changes>### 2026-09-12 (v0.10.1)
- Notifications: a VIEWER key that cannot create Unraid notifications now gets an explicit message (set NOTIFY_UNRAID_API_KEY to an ADMIN key) instead of "Forbidden resource".

### 2026-09-12 (v0.10.0)
- Web UI redesigned: dashboard counters + tabs (Files, Activity, Devices, Notifications, API keys, Advanced), readable tables, theme system/light/dark, seven languages (system/en/it/es/fr/de/zh-Hans/ar), login form that password managers (iCloud Keychain, Chrome, Firefox) can save and fill.

### 2026-09-11 (v0.9.0)
- Device registry: every Unraid Drive installation registers itself; the web UI (Devices) and `gw devices` list them; removing one (or all) closes its sessions and the app asks to sign in again.
- Notifications for new/removed devices via Unraid notifications (NOTIFY_UNRAID), e-mail (SMTP_*) and Telegram (TELEGRAM_*); test from the web UI or `gw notify-test`.
- Unraid API keys from the web UI (ADMIN key): list, create, delete, rotate (new key shown once, optionally remembered for the web UI, old key deleted).
- Console walkthrough rewritten: what the gateway is, how access works, every command, where files live.

### 2026-09-11 (v0.8.0)
- Web UI: the API key can be remembered on the gateway (`WEBUI_API_KEY` variable or "Remember this key on the gateway" → /config/webui.key) for one-click login; "Forget stored key" to remove it.
- Console: `gw activity` (and `gw activity -w` live) prints connected devices, streams/uploads in progress and recent transfers; `?format=text` on the activity API; loopback access from the container console needs no token.

### 2026-09-11 (v0.7.0)
- Activity panel in the web UI: who is connected (device, Unraid user, IP, since/last seen, requests, last file), streams and transfers in progress with progress bar and speed, recent transfers. `GET /api/v1/activity`. Sessions opened with an Unraid user see only their own activity; API-key-only and ADMIN sessions see everyone.

### 2026-09-11 (v0.6.0)
- Media tickets: `POST /api/v1/fs/ticket` gives a signed, expiring URL (`/media/<ticket>`) for one file, so players that cannot send headers (the mpv player of Unraid Drive on Apple TV, VLC, a browser) can stream with Range support. No credential in the URL; tickets die with a restart.

### 2026-09-11 (v0.5.5)
- Files and folders created through the gateway now get Unraid's standard permissions (nobody:users, 0777/0666, as Tools › New Permissions), so other Unraid users and containers can always modify them.
- "Permission denied" errors now say which folder, its owner and mode, and what to do (Tools › New Permissions); a warning at start-up lists shares the gateway cannot write to.

### 2026-09-09 (v0.5.4)
- Final icon (author's artwork, light and dark).

### 2026-09-09 (v0.5.0)
- Persistent item index (SQLite in /config): stable ids per file, change journal, instant `/fs/changes?seq=` feed, `/fs/item?id=`; background scanner (directory scan every 5 min, full scan every 6 h). Mount /mnt/user/appdata/unraid-gateway to /config.

### 2026-09-09 (v0.4.0)
- Readable text logs by default (LOG_FORMAT/LOG_LEVEL/LOG_HEALTHCHECKS), start-up banner, console walkthrough (`gw` command).

### 2026-09-08 (v0.3.x)
- Per-user access: Unraid username and password verified over SMB, share permissions (public/secure/private, read and write lists) applied as in Unraid; shares are the real container mounts.

### 2026-09-08 (v0.2.x)
- Paginated change feed for large shares, immutable share roots, resumable uploads, web UI.

### 2026-09-08 (v0.1.0)
- First release.
  </Changes>
  <Requires>An Unraid API key (Settings &gt; Management Access &gt; API Keys). For remote access, a reverse proxy with a valid TLS certificate or Cloudflare Tunnel in front of the gateway port.</Requires>
  <Config Name="Gateway port" Target="8484" Default="8484" Mode="tcp" Description="Port exposed by the gateway. Put a reverse proxy with TLS in front of it before exposing it to the Internet." Type="Port" Display="always" Required="true" Mask="false">8484</Config>
  <Config Name="Unraid WebGUI URL" Target="UNRAID_URL" Default="http://192.168.1.10" Mode="" Description="LAN URL of your Unraid WebGUI, used to validate API keys and proxy GraphQL. Use the server IP, not localhost." Type="Variable" Display="always" Required="true" Mask="false">http://192.168.1.10</Config>
  <Config Name="documents share" Target="/data/documents" Default="/mnt/user/documents" Mode="rw" Description="Each mapped share appears as a top-level folder in the Files app. Add more shares with 'Add another Path' (container path /data/&lt;name&gt;); set Access Mode to Read Only for shares you only want to browse." Type="Path" Display="always" Required="false" Mask="false">/mnt/user/documents</Config>
  <Config Name="media share" Target="/data/media" Default="/mnt/user/media" Mode="rw" Description="" Type="Path" Display="always" Required="false" Mask="false">/mnt/user/media</Config>
  <Config Name="downloads share" Target="/data/downloads" Default="/mnt/user/downloads" Mode="rw" Description="" Type="Path" Display="always" Required="false" Mask="false">/mnt/user/downloads</Config>
  <Config Name="Gateway data (item index)" Target="/config" Default="/mnt/user/appdata/unraid-gateway" Mode="rw" Description="Small SQLite catalogue of your files (stable ids, change journal) so the app syncs instantly instead of walking the shares. Safe to delete: it is rebuilt at start." Type="Path" Display="always" Required="false" Mask="false">/mnt/user/appdata/unraid-gateway</Config>
  <Config Name="Unraid share security (read-only)" Target="/unraid-shares/smb-shares.conf" Default="/etc/samba/smb-shares.conf" Mode="ro" Description="The share definitions Unraid generates for Samba (public/secure/private, read and write user lists). Needed so that users logging in with their Unraid username and password get exactly the permissions they have over SMB." Type="Path" Display="always" Required="false" Mask="false">/etc/samba/smb-shares.conf</Config>
  <Config Name="Web UI stored API key (optional)" Target="WEBUI_API_KEY" Default="" Mode="" Description="An Unraid API key the web UI on port 8484 can use with one click, so you do not paste it at every visit. Leave empty and tick 'Remember this key on the gateway' in the web UI instead (saved in /config/webui.key). Anyone who can open the web UI can then use the key: keep the gateway on the LAN or behind Cloudflare Access." Type="Variable" Display="advanced" Required="false" Mask="true"></Config>
  <Config Name="Device registration" Target="DEVICE_REGISTRATION" Default="on" Mode="" Description="on: every Unraid Drive installation registers itself at first sign-in and can be revoked from the web UI (Devices) or gw devices; off: no registry." Type="Variable" Display="advanced" Required="false" Mask="false">on</Config>
  <Config Name="Notify via Unraid" Target="NOTIFY_UNRAID" Default="true" Mode="" Description="Announce new/removed devices through Unraid's notification system (uses the signing-in key; set 'Notify Unraid API key' to an ADMIN key if VIEWER keys are refused)." Type="Variable" Display="advanced" Required="false" Mask="false">true</Config>
  <Config Name="Notify Unraid API key (optional)" Target="NOTIFY_UNRAID_API_KEY" Default="" Mode="" Description="ADMIN API key used only to create Unraid notifications." Type="Variable" Display="advanced" Required="false" Mask="true"></Config>
  <Config Name="SMTP host (optional)" Target="SMTP_HOST" Default="" Mode="" Description="E-mail notifications: SMTP server. Also set SMTP_PORT (587), SMTP_USER, SMTP_PASSWORD, SMTP_FROM, SMTP_TO, SMTP_TLS (starttls|tls|none)." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="SMTP port" Target="SMTP_PORT" Default="587" Mode="" Description="" Type="Variable" Display="advanced" Required="false" Mask="false">587</Config>
  <Config Name="SMTP user" Target="SMTP_USER" Default="" Mode="" Description="" Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="SMTP password" Target="SMTP_PASSWORD" Default="" Mode="" Description="" Type="Variable" Display="advanced" Required="false" Mask="true"></Config>
  <Config Name="SMTP from" Target="SMTP_FROM" Default="" Mode="" Description="Sender address (defaults to SMTP user)." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="SMTP to" Target="SMTP_TO" Default="" Mode="" Description="Recipient(s), comma separated." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="SMTP TLS" Target="SMTP_TLS" Default="starttls" Mode="" Description="starttls (587) | tls (465) | none" Type="Variable" Display="advanced" Required="false" Mask="false">starttls</Config>
  <Config Name="Telegram bot token (optional)" Target="TELEGRAM_BOT_TOKEN" Default="" Mode="" Description="Telegram notifications: token from @BotFather; also set TELEGRAM_CHAT_ID." Type="Variable" Display="advanced" Required="false" Mask="true"></Config>
  <Config Name="Telegram chat id" Target="TELEGRAM_CHAT_ID" Default="" Mode="" Description="Chat or channel id that receives the messages." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="User authentication" Target="USER_AUTH" Default="optional" Mode="" Description="optional: clients may add an Unraid username and password to the API key and get that user's share permissions; required: every client must; off: API key only (access = container mounts)." Type="Variable" Display="always" Required="false" Mask="false">optional</Config>
  <Config Name="Unraid SMB address" Target="UNRAID_SMB_ADDR" Default="" Mode="" Description="host:port of the Unraid SMB service used to verify user passwords. Empty = host of the WebGUI URL, port 445." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="Read-only mode" Target="READ_ONLY" Default="false" Mode="" Description="true disables every write operation on all mapped shares." Type="Variable" Display="advanced" Required="false" Mask="false">false</Config>
  <Config Name="Session lifetime" Target="SESSION_TTL" Default="12h" Mode="" Description="How long a login token stays valid (Go duration: 30m, 12h, 168h)." Type="Variable" Display="advanced" Required="false" Mask="false">12h</Config>
  <Config Name="Max login attempts" Target="MAX_LOGIN_ATTEMPTS" Default="5" Mode="" Description="Failed logins per IP before a temporary lockout." Type="Variable" Display="advanced" Required="false" Mask="false">5</Config>
  <Config Name="Login lockout" Target="LOGIN_LOCKOUT" Default="15m" Mode="" Description="Lockout duration after too many failed logins." Type="Variable" Display="advanced" Required="false" Mask="false">15m</Config>
  <Config Name="Trust proxy headers" Target="TRUST_PROXY" Default="false" Mode="" Description="true when behind Nginx Proxy Manager, SWAG or Cloudflare Tunnel, so rate limiting sees the real client IP." Type="Variable" Display="advanced" Required="false" Mask="false">false</Config>
  <Config Name="Unraid self-signed TLS" Target="UNRAID_INSECURE_TLS" Default="false" Mode="" Description="true only if UNRAID_URL is https with a self-signed certificate." Type="Variable" Display="advanced" Required="false" Mask="false">false</Config>
  <Config Name="Validation query" Target="UNRAID_VALIDATE_QUERY" Default="query { me { id name roles } }" Mode="" Description="GraphQL query used to validate API keys. Change only if your Unraid version lacks the 'me' field." Type="Variable" Display="advanced" Required="false" Mask="false">query { me { id name roles } }</Config>
  <Config Name="Index directory scan" Target="INDEX_DIR_SCAN" Default="5m" Mode="" Description="How often directories are checked for changes made outside the gateway (SMB, other containers). Only directories are stat'ed." Type="Variable" Display="advanced" Required="false" Mask="false">5m</Config>
  <Config Name="Index full scan" Target="INDEX_FULL_SCAN" Default="6h" Mode="" Description="How often every file is re-checked (catches content edits that leave the folder date untouched)." Type="Variable" Display="advanced" Required="false" Mask="false">6h</Config>
  <Config Name="Change feed page deadline" Target="CHANGES_DEADLINE" Default="20s" Mode="" Description="Max time per /fs/changes page. Large shares on shfs scan at roughly 3000 entries per second; clients page with after=next." Type="Variable" Display="advanced" Required="false" Mask="false">20s</Config>
  <Config Name="Timezone" Target="TZ" Default="Europe/Rome" Mode="" Description="" Type="Variable" Display="advanced" Required="false" Mask="false">Europe/Rome</Config>
</Container>
